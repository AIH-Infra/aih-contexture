# VLM 档案编号提取 - 调试清单

## 问题现状

✅ 配置正确：
- 转换模式：VLM Direct
- 提示词模板：档案文献
- 启用页码锚点：是
- 提取印刷页码：是
- 正则表达式：`<!--\s*printed-page:\s*(.+?)\s*-->`

❌ 结果：没有提取出任何页码

---

## 调试步骤

### 步骤 1: 检查 VLM 原始输出

**操作**:
1. 在 Streamlit 界面运行转换
2. 转换完成后，查看 "Markdown 输出" 或 "转换结果"
3. 复制前 500 个字符的输出

**检查点**:
- [ ] 输出开头是否有 `<!-- printed-page: SC 001 -->` 这样的标记？
- [ ] 如果没有，是否有其他类似的标记（如 `<!-- page:`, `页码:` 等）？
- [ ] 如果完全没有任何标记，说明 VLM 没有输出

**如果找到标记**:
→ 记录标记的确切格式，我们可以调整正则表达式

**如果没有找到任何标记**:
→ 说明 VLM 没有识别档案编号，继续下一步

---

### 步骤 2: 检查提示词是否传递给 VLM

**操作**:
1. 查看终端/控制台日志
2. 搜索 `[VlmDirectAsyncConverter]` 相关的日志

**检查点**:
- [ ] 是否有 `Using template: archive_document` 日志？
- [ ] 是否有提示词相关的日志？

**如果没有日志**:
→ 说明配置没有正确传递

---

### 步骤 3: 检查档案图片

**操作**:
1. 打开你上传的档案图片
2. 仔细查看页面上的编号

**检查点**:
- [ ] 编号是否清晰可见？
- [ ] 编号位置在哪里（顶部/底部/角落）？
- [ ] 编号格式是什么（SC 001, SC-001, 其他）？
- [ ] 编号字体大小如何？

**如果编号不清晰**:
→ VLM 可能看不到，建议使用自动生成模式

---

## 可能的原因和解决方案

### 原因 1: VLM 没有输出标记

**症状**: VLM 输出中完全没有 `<!-- printed-page:` 标记

**可能原因**:
1. 提示词模板没有正确传递
2. VLM 模型能力不足，忽略了指令
3. VLM 没有看到档案编号

**解决方案**:
```
方案 A: 使用自动生成模式
1. 自定义编号来源: 自动生成
2. 编号前缀: SC
3. 起始编号: 1
4. 分隔符: 空格
5. 编号位数: 3

方案 B: 手动输入列表（临时方案）
1. 自定义编号来源: 手动输入列表
2. 输入: SC 001, SC 002, SC 003, ...
```

---

### 原因 2: VLM 使用了不同的输出格式

**症状**: VLM 输出中有页码信息，但格式不是 `<!-- printed-page:`

**可能格式**:
- `<!-- page: SC 001 -->` (旧格式)
- `页码: SC 001` (中文格式)
- `[页码: SC 001]` (方括号格式)
- `Page: SC 001` (英文格式)

**解决方案**:
→ 告诉我 VLM 实际使用的格式，我会调整正则表达式

---

### 原因 3: 正则表达式有问题

**症状**: VLM 输出了正确的标记，但正则没有匹配

**测试**:
```python
import re
pattern = r"<!--\s*printed-page:\s*(.+?)\s*-->"
text = "<!-- printed-page: SC 001 -->"
match = re.search(pattern, text)
print(match.group(1) if match else "无法匹配")
```

**解决方案**:
→ 提供 VLM 的实际输出，我会修复正则

---

## 立即行动

**请现在做这件事**:

1. 运行一次转换
2. 复制 VLM 输出的前 500 个字符
3. 粘贴给我

这样我就能准确诊断问题所在。

---

## 备选方案（如果 VLM 提取失败）

如果 VLM 无法自动提取档案编号，可以使用：

### 方案 1: 自动生成（推荐）

**优点**: 简单、快速、可靠
**缺点**: 不是实际的档案编号

**配置**:
```
自定义编号来源: 自动生成
编号前缀: SC
起始编号: 1
分隔符: 空格
编号位数: 3
```

**结果**: SC 001, SC 002, SC 003, ...

### 方案 2: 上传 CSV 文件

**优点**: 可以使用实际的档案编号
**缺点**: 需要手动准备文件

**CSV 格式**:
```csv
0,SC 001
1,SC 002
2,SC 003
```

### 方案 3: 手动输入列表

**优点**: 灵活
**缺点**: 页数多时很麻烦

**输入格式**:
```
SC 001, SC 002, SC 003, SC 004, ...
```

---

## 总结

**当前最需要的信息**:
→ VLM 的原始输出（前 500 字符）

有了这个信息，我就能准确判断问题并提供解决方案。
